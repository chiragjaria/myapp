trigger:
- main

variables:
  acrName: myacrdevcj
  imageName: fastapi-app

stages:

# ================= CI =================
- stage: Build
  displayName: Build & Push Image

  jobs:
  - job: BuildJob
    pool:
      vmImage: ubuntu-latest   # ✅ hosted agent

    steps:
    - checkout: self

    # ✅ Build & Push Docker Image to ACR
    - task: Docker@2
      displayName: Build and Push Image
      inputs:
        command: buildAndPush
        repository: $(imageName)
        dockerfile: Dockerfile
        containerRegistry: dregistry   # your ACR service connection
        tags: |
          $(Build.BuildId)
          latest

    # ✅ Publish ONLY k8s manifests
    - task: PublishBuildArtifacts@1
      displayName: Publish k8s manifests
      inputs:
        pathToPublish: $(System.DefaultWorkingDirectory)/k8s
        artifactName: manifests


# ================= CD =================
- stage: Deploy
  displayName: Deploy to AKS
  dependsOn: Build

  jobs:
  - deployment: DeployToAKS
    pool:
      vmImage: ubuntu-latest   # ✅ hosted agent

    environment: dev

    variables:
    - group: AKV-Variables   # contains phost, puser, ppassword, pdb

    strategy:
      runOnce:
        deploy:
          steps:

          - download: current
            artifact: manifests

          # ✅ Login & get AKS credentials
          - task: AzureCLI@2
            displayName: Get AKS Credentials
            inputs:
              azureSubscription: azure-service-connection
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group rgp-dev-mes \
                  --name aks-mes-dev \
                  --overwrite-existing

          # ✅ Create Kubernetes Secret from AKV values
          - script: |
              kubectl create secret generic db-secret \
                --from-literal=DB_HOST=$(phost) \
                --from-literal=DB_NAME=$(pdb) \
                --from-literal=DB_USER=$(puser) \
                --from-literal=DB_PASS=$(ppassword) \
                --namespace=default \
                --dry-run=client -o yaml | kubectl apply -f -
            displayName: Create DB Secret

          # ✅ Deploy application
          - task: KubernetesManifest@1
            displayName: Deploy to AKS
            inputs:
              action: deploy
              namespace: default
              manifests: |
                $(Pipeline.Workspace)/manifests/deployment.yaml
                $(Pipeline.Workspace)/manifests/service.yaml
              containers: |
                $(acrName).azurecr.io/$(imageName):$(Build.BuildId)
