apiVersion: v1
kind: Service
metadata:
  name: fastapi-app-svc
  namespace: default
spec:
  selector:
    app: fastapi-app
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer   # gives you a public IP to test with
